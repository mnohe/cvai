= CVAI UX Strategy
:toc:
:toc-placement!:
:toclevels: 2

toc::[]

---

== Design philosophy

CVAI is a tool for a high-stakes, time-pressured activity — job searching.
The UX must feel like a trusted analyst sitting next to the user, not an admin panel.

Three principles drive every decision:

. *Verdict first.* The AI's judgement (CLEAR FIT / FIT / PARTIAL / NO FIT) is the most important output.
It appears at the top of every role view, in large type, before any supporting detail.
. *Act, don't browse.* Every screen surfaces the one thing the user should do next.
Gaps become tasks with one click. Statuses change inline. Nothing is buried in sub-menus.
. *Quiet confidence.* The visual language is clean and restrained.
Colour is used only to signal meaning (verdict, status, action), never decoration.

---

== Design system

=== Colour palette

Derived directly from the logo mark:

[cols="1,1,3", options="header"]
|===
| Token | Hex | Usage

| `--navy` | `#0b3954` | Base brand colour
| `--steel` | `#bfd7ea` | Base brand colour
| `--coral` | `#ff5a5f` | Base brand colour; logo diamond on the I
| `--bg` | `#f4f8fb` | Page/app background
| `--surface` | `#ffffff` | Card and panel surfaces
| `--ghost-bg` | `#eef3f7` | Ghost/secondary button fill
| `--border` | `#dce8f2` | All borders and dividers — 1 px, no box shadows anywhere
| `--text-1` | `#0b3954` | Body text (same as navy)
| `--text-2` | `#4a6b7c` | Secondary / label text
|===

Component CSS uses semantic aliases over the base palette tokens. For example:

[cols="1,2", options="header"]
|===
| Semantic token | Usage

| `--primary-action-bg` | Primary non-AI buttons
| `--secondary-action-bg` | Secondary / ghost buttons
| `--ai-action-marker-bg` | ThinkButton external-AI diamond only
| `--section-tab-bg-active` | Active Profile tab background
| `--section-panel-top-border` | Top border of the panel controlled by Profile tabs
|===

=== Verdict colours

Verdict labels use their own semantic colours, separate from the brand palette:

[cols="1,1,2", options="header"]
|===
| Label | Hex | Context

| CLEAR FIT | `#0e7c3e` | Strong match; hex-shaped block with diamond left accent on role detail
| FIT | `#1565c0` | Solid match with minor gaps; blue text
| PARTIAL | `#b45309` | Significant gaps present; amber text
| NO FIT | `#b91c1c` | Hard blockers present; red text
| Analysing… | `--text-2` | In-flight LLM assessment; shown with steel progress bar
|===

=== Typography

System font stack: `-apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif`

No custom font dependency at launch. Inter is the design target and can be swapped in once the
product is stable.

[cols="1,1,1", options="header"]
|===
| Role | Size | Weight

| Verdict label | 20–22 px | 700
| Card title / section heading | 16 px | 600
| Body text | 13–16 px | 400–500
| Labels (uppercase) | 10–12 px | 600, letter-spacing 0.08 em
| Badges | 10–11 px | 600
|===

=== Shape language

The geometric logo — specifically the 2:1 tall coral diamond on the I — informs every shape in the UI.
The proportions are invariant: all diamonds and rhombuses are 2 units tall by 1 unit wide.

*No rounded corners anywhere.* A global reset of `border-radius: 0` applies to every element.
Circular or pill shapes are replaced by diamonds or sharp rectangles as described below.

*`ThinkButton` — LLM / external-request actions only.*
Only actions that invoke the LLM (and therefore call an external provider) use the `ThinkButton` component.
The button itself is a normal sharp rectangle, matching the rest of the action system.
Its distinguishing mark is a coral diamond attached to the right edge, rendered by the component;
the diamond is the only in-button signal that the action uses external AI.
The component is named `ThinkButton` so the name describes the semantic — it triggers inference — and survives a future style change.

```css
.think-button {
  border: 0;
  height: 32px;
}
.think-button-marker {
  clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
  background: var(--ai-action-marker-bg);
}
.think-button-primary { background: var(--primary-action-bg);   color: var(--primary-action-text); }
.think-button-ghost   { background: var(--secondary-action-bg); color: var(--secondary-action-text); }
```

*Button actions.* Any button actions should disable the button immediately, and enable it only when it is safe to do so, e.g. the operation has completed, or failed to complete in a given amount of time. In the latter case, it should be accompanied by an error message whose dismissal enables the button back. For instance, a "Save" button should be disabled immediately on click, and only enabled once the form can be saved again, i.e. it has changes. If the form fails to be saved, a message close to the button (or a modal) should indicate why saving did not succeed, and dismissing the message should re-enable the button.

*Long-running button actions.* Any button that starts an operation which may take noticeable time must enter a local pending state immediately: disable the button (per the previous rule), show an in-button spinner, and keep the button disabled until the operation completes, fails, or times out. Completion removes the spinner and re-enables the button unless another gate still applies. Failure or timeout should show a specific, actionable message under the button or in the page's notification area; do not replace the button label with a persistent success or error message.

*Verdict block.* The CLEAR FIT / FIT / etc. block is a rectangle.
A 4 px left border in the verdict colour acts as the accent — thicker than the 1.5 px
perimeter border, visually distinct without a decorative element.

```css
.verdict {
  border: 1.5px solid var(--verdict-bdr);
  border-left: 4px solid var(--verdict-color);
}
```

*Panels and cards* are plain sharp rectangles — no decorative corners or accents.
The content (verdict colour, bullet colour) carries all the meaning.

*Rhombus bullets.* List markers (strengths, gaps, any `<ul>`) use a small 2:1 diamond shape
via an empty `<span>` with `clip-path`. Green for positive items, danger red for negative.

```css
.rh { width: 6px; height: 12px; flex-shrink: 0; margin-top: 2px;
      clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%); }
.rh-ok  { background: var(--clear); }
.rh-bad { background: var(--danger); }
```

*Small diamond indicators.* Status dots, nav indicators, and timeline markers
are all 2:1 rhombuses using the same `clip-path` pattern scaled to context
(nav: 7 × 14 px; timeline: 6 × 12 px; status dot: 6 × 12 px).

*Profile section tabs.* Profile tabs are flat section selectors, not buttons.
They have no border, shadow, or motion. Hover uses a pale version of the active tab background.
The active tab background and the top border of the panel below both use `--section-tab-bg-active`.

*Avatar.* The user avatar is a 2:1 rhombus (18 × 36 px) using the same clip-path.

*Status pill* is a sharp rectangle (no rounding).

=== Colour themes

Two themes are defined. The browser switches automatically via `prefers-color-scheme`.
No manual toggle is provided at launch.

All colours are CSS custom properties on `:root`. Dark-mode overrides live in a single
`@media (prefers-color-scheme: dark)` block; semantic tokens (`--clear`, `--coral`, `--steel`)
stay the same across both themes unless contrast requires adjustment.

[cols="1,1,1", options="header"]
|===
| Token | Light | Dark

| `--bg` | `#f4f8fb` | `#0a1929`
| `--surface` | `#ffffff` | `#0f2336`
| `--sidebar` | `#0b3954` | `#071622`
| `--border` | `#dce8f2` | `#1a3348`
| `--ghost-bg` | `#eef3f7` | `#162d42`
| `--text-1` | `#0b3954` | `#e2eef8`
| `--text-2` | `#4a6b7c` | `#7baac7`
| `--clear` | `#0e7c3e` | `#34d399`
| `--clear-bg` | `#f0fdf4` | `#052316`
| `--clear-bdr` | `#bbf7d0` | `#064e2a`
|===

The mobile top-nav logo uses `currentColor` on the C/V paths with a `--cv-color` token
(`#0b3954` light, `#ffffff` dark) so the mark reads on both the white light nav and the
dark surface dark nav. The sidebar logo always uses `fill="white"` since the sidebar is
always dark.

=== External AI signal

The remaining external-request capacity is not shown in the persistent chrome
(sidebar, top nav). If a deployment exposes limits, they belong in the account
panel — triggered by clicking the user avatar / name in the sidebar footer
(desktop) or the avatar diamond in the top nav (mobile).

This keeps the main task screen focused on the work. The `<ThinkButton>`
component's right-edge diamond is the only in-context signal that an action calls
an external AI provider.

=== Logo mark

The brand mark is the geometric CVAI monogram. It is self-sufficient without a wordmark text.

* On dark backgrounds (navy sidebar): C and V letterform paths are rendered `fill="white"`. A and I remain steel blue (`#bfd7ea`). The I-dot diamond is coral (`#ff5a5f`).
* On light backgrounds (white top nav on mobile web): C and V are navy (`#0b3954`).

The wordmark text "CVAI" is not shown alongside the mark — the mark spells the name.

---

== Key screens

=== Desktop — Role detail (900 × 600 reference layout)

Mockup: `mockups/cvai-desktop.html`

The hero screen of the product. Demonstrates the full information hierarchy for an assessed role.

[cols="1,3", options="header"]
|===
| Region | Content

| Left sidebar (168 px, `--sidebar`) | Logo mark (white C/V on dark); nav (Dashboard, Roles, CV, Tasks, Account); user avatar + name (opens account panel)
| Top bar | Breadcrumb ← Roles / Company · Role title; status pill (sharp rectangle with diamond dot); Reassess ghost `ThinkButton`; Create gap tasks primary `ThinkButton`
| Verdict block | Hex-shaped, diamond left accent, CLEAR FIT label at 22 px, sub-text, recommendation
| Strengths / Gaps columns | Two panels side by side; checkmarks (green) and × marks (danger red); gap footer with effort estimate
| Requirements coverage table | Requirement / Coverage badge (✓ met, ~ partial, ✗ open) / Evidence reference
| Artifact links | Ghost buttons: Suitability report, Role matrix, View source posting
| Timeline | Chronological events with status-change tags
|===

=== Mobile web — Role detail at 390 px

Mockup: `mockups/cvai-mobile-web.html`

The same role detail content at a narrow phone-browser width.
This is a *responsive website*, not a native app: white top bar, no bottom nav, no FAB.

[cols="1,3", options="header"]
|===
| Region | Content

| Top nav | Hamburger ☰ (left); centred logo mark (C/V in `--cv-color`, adapts to theme); steel diamond avatar (right, opens account panel)
| Breadcrumb row | ← Roles / Company · Role title
| Role header | Title; company · location meta; sharp-rectangle status pill + ThinkButton actions inline
| Verdict block | Full-width rectangle with 2:1 rhombus jewel on left edge, identical logic to desktop
| Strengths panel | Full width (stacked, not side-by-side)
| Gaps panel | Full width
| Requirements table | Full width; horizontally scrollable if needed
| Artifact links | Full-width sharp-rectangle buttons (plain link style, not hex)
|===

=== Legacy mobile app mockup — Role list

Mockup: `mockups/cvai-mobile.html` (reference / archived)

Native-app style role list with dark header, tab bar, FAB, and bottom nav.
No mobile app is planned at launch — this is retained as a design reference only.

---

== Interaction patterns

=== Progress feedback (Analysing state)

When an LLM action is in flight, the card/page shows "Analysing…" with a steel progress bar.
The SPA subscribes to the Firestore Action document via `onSnapshot`; no polling.
On `complete`, the card updates in-place without a page reload.
See `GA_IMPL.adoc §Cross-cutting patterns — Action subscription pattern`.

=== Status transitions

The status pill in the top bar is an inline dropdown (fully rounded, not hex).
Changing status writes directly to the Role document in Firestore — no save button.
Available transitions: Interested → Applied → Screening → Interview → Offer → Accepted / Closed.

=== Gap tasks

"+ Create gap tasks" reserves an external request and runs the gap-to-task LLM function.
The button becomes disabled while the action is in flight and re-enables on completion.
Generated tasks appear in the Tasks section and are linked back to the originating role.

=== Empty states

* No roles yet: navy illustration + "Add your first role" CTA
* No CV uploaded: banner above role list prompting CV upload before first analysis
* Analysing (first role): full-page progress state, not a card, to set expectations

---

== Profile completion meter

The completion meter tracks how much of the candidate profile has been filled in.
It is a prerequisite gate: analytics features (Quick Analysis, Generate Bundle) are disabled until the profile reaches the green threshold (≥ 2/5).

=== Segments

[cols="1,1,3", options="header"]
|===
| # | Segment | Satisfied when

| 1 | Personal data | `candidate.cv.personal` has at least a name and one contact field
| 2 | Work experience | `candidate.cv.experience` has at least one entry
| 3 | Story | `candidate.storyBank` has at least one entry
| 4 | Education | `candidate.cv.education` has at least one entry
| 5 | Portfolio evidence | `candidate.evidenceLibrary` has at least one entry
|===

=== Status colours

[cols="1,1", options="header"]
|===
| Filled segments | State

| 0 | Red — no analytics, no role analysis
| 1 | Amber — profile started, analytics still locked
| ≥ 2 | Green — analytics unlocked
| 5 | Hidden — meter disappears; profile is complete
|===

=== Placement

The meter sits in the sidebar footer on desktop, immediately to the left of the user's display name.
On mobile it appears inside the account panel (opened from the avatar diamond in the top nav), not in the persistent chrome.

Desktop sidebar footer (incomplete profile, 2/5 green):

----
  A. User  ◆ ◆ ◇ ◇ ◇  
  auser@example.com
----

The meter is five 2:1 rhombuses in a row, using the same `clip-path` as every other diamond in the
design system (`polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%)`).
Each pip is 6 × 12 px with 3 px horizontal gap. Filled pips use the status colour; empty pips use `--border`.

[source,css]
----
.meter-pip {
  width: 6px; height: 12px;
  clip-path: polygon(50% 0%, 100% 50%, 50% 100%, 0% 50%);
}
.meter-pip--filled { background: var(--meter-color); }
.meter-pip--empty  { background: var(--border); }
----

`--meter-color` maps to the status: red (`#b91c1c`) at 0/5, amber (`#b45309`) at 1/5, green (`var(--clear)`) at ≥ 2/5.

When the profile is 5/5 the component renders `null` — no placeholder, no space.

=== Interaction

Clicking the meter (desktop) or tapping it (mobile account panel) navigates to `/profile/cv` with the
profile completion panel expanded, showing each segment's label, status, and a direct link to where
it can be satisfied.

=== Analytics gate

`<ThinkButton>` for Quick Analysis and Generate Bundle renders as disabled with a tooltip —
"Complete your profile first (2 of 5 required)" — when the meter is below green.
The button is never hidden, only disabled; the user can see what they are working toward.

---

== SPA component inventory

Components are Vite + React 19 + TypeScript + Tailwind.

[cols="1,2,1", options="header"]
|===
| Component | Description | Milestone

| `<SidebarNav>` | Desktop persistent sidebar with logo mark (white C/V on navy), nav, user | M1
| `<TopNav>` | Mobile-web white top bar: hamburger, centred logo, avatar diamond | M1
| `<ProfileCompletionMeter>` | Row of five 2:1 rhombus pips; same clip-path as all other diamonds; red/amber/green by filled count; hidden at 5/5; click navigates to `/profile/cv` completion panel; drives analytics gate | M1
| `<VerdictBlock>` | Rectangle with 4 px left border in verdict colour; 1.5 px perimeter border in verdict tint | M3
| `<ThinkButton>` | LLM/external-request actions only; normal sharp button with a coral right-edge diamond marker; variant="primary" (navy) or variant="ghost" (ghost-bg); disabled with tooltip when profile below green threshold | M1
| `<StrengthGapPanel>` | Strengths or gaps panel; two-column on desktop, stacked on mobile | M3
| `<RequirementsTable>` | Coverage table with badge cells (✓ met / ~ partial / ✗ open) | M3
| `<RoleCard>` | Role list card with verdict label, status badge, and meta line | M3
| `<StatusPill>` | Inline status dropdown for role lifecycle; sharp rectangle with 2:1 diamond dot indicator | M3
| `<ActionProgress>` | "Analysing…" state with steel progress bar; Firestore onSnapshot wired | M1
| `<TabFilterBar>` | Horizontal scrollable filter tabs (role list) | M3
| `<Timeline>` | Vertical event list with date, dot, and status tag | M3
|===

---

== Development conventions

=== Testing selectors

Every HTML element that renders a discrete data value must carry a stable `id` or semantic `class` attribute so it can be targeted by automated tests without relying on text content or DOM position.

Apply this rule to all new elements going forward.
Do not apply it retroactively to existing markup unless the element is already being touched by the change at hand.

Guidelines:

* Prefer a `class` for repeated patterns (e.g. `.role-title`, `.verdict-label`, `.position-period`).
* Use `id` only when an element is guaranteed to be unique on the page (e.g. `id="profile-completion-meter"`).
* Avoid encoding state or value into the selector name — select *what* the element is, not *what it contains* (`.company-name`, not `.company-acme`).
* Attribute the class at the element that owns the data, not a layout wrapper around it.
